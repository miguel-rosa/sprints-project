import React from "react";
import styles from "./Sprint.module.css"

const Header = () => {
  return (
    <header className={styles.header}>
      <h1 className={styles.logo}>LOREM</h1>
      <h4 className={styles.fiapLogo}>FIAP</h4>
    </header>
  )
}

export default Header;